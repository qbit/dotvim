Switching my vim stuff to use pathogen

Get: 
	git clone git@github.com:qbit/dotvim.git ~/.vim
	cd ~/.vim
	git submodule init
	git submodule update

256colors.pl:
	used to test for 256 colors in a terminal

SubModules:
	JSLint
	NERDTree
	perlomni
	snipMate
	tComment
	vimwiki
